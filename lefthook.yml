pre-commit:
  parallel: true
  commands:
    quick-check:
      run: pnpm quick-check
    root-hygiene:
      run: python3 scripts/verify_root_hygiene.py
    secretlint:
      glob: "*.{ts,tsx,js,jsx,json,yml,yaml,env,md,sh,toml}"
      run: pnpm exec secretlint {staged_files}
